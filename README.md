# Информационная система трудоустройства студентов

Платформа для автоматизации процесса трудоустройства выпускников вузов Казахстана. Система связывает студентов, работодателей и университеты в едином цифровом пространстве.

---

## Архитектура системы

```
                        ┌─────────────────┐
                        │    Браузер      │
                        │  React + TS     │
                        │   :3000         │
                        └────────┬────────┘
                                 │ HTTP
                        ┌────────▼────────┐
                        │   API Gateway   │
                        │   Gin (Go)      │
                        │   :8000         │
                        └────────┬────────┘
                                 │
            ┌────────────────────┼────────────────────┐
            │ HTTP               │ gRPC               │ gRPC
   ┌────────▼────────┐  ┌────────▼────────┐  ┌────────▼────────┐
   │  Auth Service   │  │ Student Service │  │Employer Service │
   │   Gin (Go)      │  │ gRPC + HTTP     │  │   gRPC (Go)     │
   │   :8081         │  │ :50051 / :8082  │  │   :50052        │
   └────────┬────────┘  └────────┬────────┘  └────────┬────────┘
            │                    │                     │
   ┌────────▼────────┐  ┌────────▼────────┐  ┌────────▼────────┐
   │   users-db      │  │   student-db    │  │   employer-db   │
   │  PostgreSQL 17  │  │  PostgreSQL 17  │  │  PostgreSQL 17  │
   │   :5432         │  │   :5433         │  │   :5434         │
   └─────────────────┘  └─────────────────┘  └─────────────────┘

            ┌────────────────────┐
            │ gRPC               │ gRPC
   ┌────────▼────────┐  ┌────────▼────────┐
   │University Svc   │  │Application Svc  │
   │   gRPC (Go)     │  │ gRPC + HTTP     │
   │   :50053        │  │ :50054 / :8083  │
   └────────┬────────┘  └────────┬────────┘
            │                    │
   ┌────────▼────────┐  ┌────────▼────────┐   ┌─────────────────┐
   │  university-db  │  │ application-db  │   │      Redis      │
   │  PostgreSQL 17  │  │  PostgreSQL 17  │   │   :6379         │
   │   :5435         │  │   :5436         │   │  (кэш, токены)  │
   └─────────────────┘  └─────────────────┘   └─────────────────┘
```

### Описание сервисов

| Сервис | Назначение | Транспорт |
|--------|-----------|-----------|
| **api-gateway** | Единая точка входа, JWT-аутентификация, маршрутизация | HTTP REST |
| **auth-service** | Регистрация, вход, email-верификация, JWT, сброс пароля | HTTP |
| **student-service** | Профиль студента, документы (CV, диплом), уведомления | gRPC + HTTP |
| **employer-service** | Профиль работодателя, вакансии, кэш вакансий в Redis | gRPC |
| **university-service** | Справочник университетов (предзаполнен) | gRPC |
| **application-service** | Заявки, собеседования, трудоустройство, чат | gRPC + HTTP |

---

## Технологический стек

**Backend:**
- Go 1.22 — все микросервисы
- gRPC + Protocol Buffers — межсервисная коммуникация
- Gin — HTTP-фреймворк (api-gateway, auth-service)
- GORM — ORM для PostgreSQL
- PostgreSQL 17 — хранение данных (5 независимых БД)
- Redis 7 — кэш вакансий, JWT-токены, коды верификации

**Frontend:**
- React 19 + TypeScript
- Vite — сборщик
- TailwindCSS — стилизация
- react-i18next — интернационализация (RU / EN / KZ)
- React Router 7

**Инфраструктура:**
- Docker + Docker Compose — контейнеризация всего стека

---

## Требования

- [Docker Desktop](https://www.docker.com/products/docker-desktop/) 24+
- [Docker Compose](https://docs.docker.com/compose/) v2
- Gmail-аккаунт с [App Password](https://myaccount.google.com/apppasswords) для отправки email

> Node.js и Go устанавливать **не нужно** — всё запускается в Docker.

---

## Быстрый старт

### 1. Клонировать репозиторий

```bash
git clone <url-репозитория>
cd Development-of-an-information-system-for-student-employment
```

### 2. Создать файл `.env`

```bash
cp .env.example .env
```

Открыть `.env` и заполнить значения:

```env
# Пароль для всех PostgreSQL баз
POSTGRES_PASSWORD=ваш_пароль

# Секрет для подписи JWT (минимум 32 символа)
JWT_SECRET=очень_длинный_и_случайный_секретный_ключ_32+

# Настройки Gmail SMTP для отправки писем
SMTP_USER=ваш_email@gmail.com
SMTP_PASSWORD=xxxx_xxxx_xxxx_xxxx   # 16-значный App Password от Google
SMTP_FROM=ваш_email@gmail.com
```

> **Как получить Gmail App Password:**
> 1. Перейти на [myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords)
> 2. Создать новый пароль для приложения
> 3. Скопировать 16-значный код в `SMTP_PASSWORD`

### 3. Запустить систему

```bash
docker compose up -d --build
```

Первый запуск занимает **3–5 минут** (сборка образов, загрузка зависимостей).

### 4. Проверить статус контейнеров

```bash
docker compose ps
```

Все контейнеры должны иметь статус `running` или `healthy`.

### 5. Открыть приложение

Перейти в браузере: **[http://localhost:3000](http://localhost:3000)**

---

## Порты сервисов

| Сервис | Порт | Описание |
|--------|------|----------|
| Frontend | `3000` | Веб-интерфейс |
| API Gateway | `8000` | REST API (основной вход) |
| Auth Service | `8081` | Аутентификация |
| Student Service HTTP | `8082` | Документы, уведомления |
| Application Service HTTP | `8083` | Собеседования, чат |
| Student Service gRPC | `50051` | gRPC |
| Employer Service gRPC | `50052` | gRPC |
| University Service gRPC | `50053` | gRPC |
| Application Service gRPC | `50054` | gRPC |
| users-db | `5432` | PostgreSQL (пользователи) |
| student-db | `5433` | PostgreSQL (студенты) |
| employer-db | `5434` | PostgreSQL (работодатели) |
| university-db | `5435` | PostgreSQL (университеты) |
| application-db | `5436` | PostgreSQL (заявки) |
| Redis | `6379` | Кэш и токены |

---

## Роли пользователей

| Роль | Возможности |
|------|------------|
| **Студент** | Создание профиля, загрузка документов, поиск и отклик на вакансии, чат с работодателем, просмотр статуса заявок |
| **Работодатель** | Создание вакансий, просмотр кандидатов, назначение собеседований, оформление трудоустройства |
| **Университет** | Верификация документов студентов, аналитика трудоустройства выпускников |

---

## Тестовый сценарий

1. Зарегистрироваться как **студент** → подтвердить email
2. Заполнить профиль (ИИН, специализация, навыки, ВУЗ)
3. Загрузить CV или диплом
4. Зарегистрироваться как **работодатель** → создать вакансию
5. Студент откликается на вакансию
6. Работодатель назначает собеседование
7. После найма работодатель создаёт запись о трудоустройстве
8. **Университет** видит аналитику на дашборде

---

## Управление контейнерами

```bash
# Запустить все сервисы
docker compose up -d

# Остановить все сервисы (данные сохраняются)
docker compose down

# Остановить и удалить все данные (сброс БД)
docker compose down -v

# Пересобрать конкретный сервис
docker compose up -d --build <имя-сервиса>

# Просмотр логов сервиса
docker compose logs -f <имя-сервиса>

# Примеры:
docker compose logs -f api-gateway
docker compose logs -f auth-service
docker compose logs -f frontend
```

---

## Структура проекта

```
.
├── services/
│   ├── api-gateway/          # Gin API Gateway (маршрутизация, JWT)
│   ├── auth-service/         # Аутентификация и авторизация
│   ├── student-service/      # Профили и документы студентов
│   ├── employer-service/     # Вакансии и профили работодателей
│   ├── university-service/   # Справочник университетов
│   └── application-service/  # Заявки, собеседования, трудоустройство, чат
├── web/                      # React + TypeScript фронтенд
│   └── src/
│       ├── pages/            # Страницы (15 страниц)
│       ├── components/       # Переиспользуемые компоненты
│       ├── services/         # API-клиенты
│       └── i18n/             # Переводы (RU / EN / KZ)
├── proto/                    # Protocol Buffer определения (gRPC)
├── docker-compose.yml        # Оркестрация всего стека
└── .env.example              # Шаблон переменных окружения
```

---

## Устранение неполадок

**Контейнер не запускается:**
```bash
docker compose logs <имя-сервиса>
```

**Список университетов пуст при регистрации:**
Перезагрузить страницу — первый запрос после холодного старта может занять 5–10 секунд.

**Email-верификация не приходит:**
Проверить правильность `SMTP_USER`, `SMTP_PASSWORD` в `.env`. Пароль должен быть App Password, не обычный пароль Google.

**Полный сброс системы (пересоздание БД):**
```bash
docker compose down -v
docker compose up -d --build
```
